# 프로젝트 전체 흐름

이 문서는 "왜 이 순서로, 왜 이런 선택을 했는가"를 하나의 이야기로 잇는 문서다. 각 단계의 세부 실행/근거는 해당 노트북에, 남은 작업 목록은 `PLAN.md`에 있다.

> **수치 기준**: 이 문서의 모든 수치는 2026-09-19 실행 결과이며, 노트북 01~08의 출력 및 `report/결과보고서.pdf`와 일치한다.

## 0. 과제의 뼈대

과제 요구사항(CLAUDE.md)을 한 문장으로 요약하면: _"토큰화·임베딩·전이학습을 어떻게, 왜 선택했는지 근거를 대고, 베이스라인 대비 무엇이 얼마나 개선됐는지 실험과 오류분석으로 증명하라"_ 이다. 그래서 이 프로젝트는 처음부터 하나의 대비 구조로 설계했다:

> **베이스라인** = 사전학습 없이 처음부터 학습하는 Transformer
> **고도화(전이학습)** = 사전학습된 KoBART/KE-T5를 파인튜닝
>
> 이 둘을 **동일한 데이터, 동일한 평가 지표**로 비교해서 "전이학습이 실제로 무엇을 얼마나 개선하는가"를 수치와 오류 사례로 보여주는 것이 프로젝트의 핵심 줄거리다.

이 줄거리를 따라가면 각 노트북이 왜 그 순서로 존재하는지 자연스럽게 설명된다. 또한 각 단계는 **"다른 조건은 고정하고 하나만 바꾼다"**는 통제 실험 원칙을 지켜, 개선이 관찰될 때 그 원인을 특정할 수 있도록 했다.

**최종 결론(미리 보기)**: 전이학습(KoBART, BLEU 7.90)이 베이스라인(4.87)을 62% 앞섰다. 다만 이 결론에 도달하기까지 **파인튜닝 파이프라인의 결함을 발견·수정하는 과정**이 있었고, 그것이 이 프로젝트에서 가장 중요한 발견이다(아래 4.5절).

---

## 1. 데이터 전처리 흐름 (`01_data_eda.ipynb`)

```
원본 로드 → 구조 확인(결측치) → 중복 확인(직접 행 확인) → 정제(공백 정규화)
→ 길이 분포 확인(percentile) → train/val/test 분할(label 비율 유지) → 저장
```

- **왜 EDA가 가장 먼저인가**: 뒤 단계(토크나이저 vocab size, 모델 max_length, 평가 시 label별 성능 해석)에서 쓸 "근거 숫자"를 여기서 확보해야 하기 때문이다. 예를 들어 Q/A 길이의 95 percentile을 몰랐다면 03번 노트북의 `MAX_LEN`을 감으로 정했을 것이다.
- **중복 제거를 (Q, A) 쌍 기준으로 한 이유**: Q만 같고 A가 다른 경우는 "같은 질문에 여러 답이 있을 수 있다"는 자연스러운 다양성이라 남겨야 하지만, (Q, A)가 완전히 같은 행은 정보량 없이 특정 패턴의 손실 가중치만 키운다.
  - (미해결) 반대로 (Q, A)는 같은데 label만 다른 행이 있다는 점도 발견했다. 현재는 label을 학습에 쓰지 않아 제거해도 무방하지만, label을 조건으로 주는 모델로 확장한다면 재검토가 필요하다. `PLAN.md`에 남겨두었다.
- **95 percentile을 max_length 기준으로 쓰는 이유**: 최댓값 기준으로 잡으면 극소수 이상치 문장 때문에 대다수의 짧은 문장이 불필요하게 padding되어 연산이 낭비된다. 95 percentile은 "이상치 5%의 일부 잘림을 감수하고 나머지 95%는 손실 없이 담는" 절충점이다. 이 기준은 03번에서 실제 `MAX_LEN`을 정할 때도 (이번엔 BPE 토큰 길이 기준으로) 그대로 재사용한다.
- **실측 결과**: 원본 11,823건 → 정제 후 11,750건(중복 73건 제거). label 분포는 일상 5,290 / 이별 3,570 / 사랑 2,963. Q 길이 95p 25음절, A 길이 95p 27음절.
- **산출물**: `dataset/processed/{train,val,test}.csv` (9,400 / 1,175 / 1,175) — 이후 모든 노트북의 공통 입력.

---

## 2. 분석 단위(토큰화) 선정 흐름 (`02_tokenization_embedding.ipynb`)

```
어절/음절/형태소/자모 비교 (정성+정량)
  → "형태소가 의미 보존과 시퀀스 길이의 절충점"이라는 결론
  → 그러나 모델 입력으로는 형태소 분석만으로 부족(신조어/오타에 약함, OOV)
  → 서브워드(SentencePiece BPE) 직접 학습으로 해결
  → 사전학습 모델(KoBART) 토크나이저와 비교
  → "베이스라인엔 직접 학습한 BPE, 전이학습 모델엔 그 모델 고유 토크나이저" 라는 분기 확정
  → Word2Vec(gensim) 임베딩 학습 및 코사인 유사도로 품질 검증
```

- **왜 네 가지 단위를 다 비교했는가**: "형태소 단위를 쓴다"는 결론이 감이 아니라 실측(평균 토큰 수: 어절 3.53 < 형태소 5.26 < 음절 10.31 < 자모 24.54)과, 실제 신조어 예시("갓생살기")에서 형태소 분석기가 깨지는 것을 눈으로 확인한 근거 위에 서 있어야 하기 때문이다.
- **BPE를 도입한 이유**: 형태소 분석기는 규칙/사전 기반이라 학습 데이터에 없던 표현에 약하다. BPE는 통계적 병합이라 어떤 문자열이든 subword 조합으로 표현할 수 있어 OOV가 사실상 사라진다 — 이건 우리가 다음 단계에서 쓸 모든 사전학습 Transformer가 공통적으로 서브워드를 쓰는 이유와 같다.
- **"왜 두 개의 토크나이저를 병행하는가"**: 이게 이 단계에서 가장 중요한 분기점이다. 사전학습 모델의 임베딩 행렬은 그 모델 고유의 vocab index에 맞춰 학습되어 있어서, 파인튜닝 시 다른 토크나이저를 쓰면 그 사전학습 지식 자체가 무의미해진다. 그래서:
  - **베이스라인(03·06번)** → 직접 학습한 `chatbot_bpe.model` (vocab 8,000)
  - **전이학습(04·06번)** → KoBART/KE-T5 각각의 `AutoTokenizer` (KoBART vocab 30,000)
  - 이 분기가 03번과 04번 노트북의 데이터 파이프라인이 서로 다른 근본 이유다.
- **Word2Vec을 별도로 학습한 이유**: "임베딩이 학습되면서 의미 공간이 어떻게 형성되는가"를 단계별(형태소분석→vocab구축→중심-주변 단어 쌍 생성→skip-gram 학습→유사도 확인)로 직접 보여주기 위함이다. 학습 단어 6,048개. 이 임베딩 자체는 전이학습에는 쓰이지 않지만, 03번 베이스라인의 임베딩 초기화 방식(랜덤 vs Word2Vec)을 비교하는 실험(06번)의 재료로 이월된다.
- **산출물**: `dataset/processed/chatbot_bpe.model`(+`.vocab`), `dataset/processed/word2vec.model`

---

## 3. 모델 구조 선정 흐름

```
"Q→A는 조건부 생성 문제" 라는 문제 정의
  → 기계번역/요약과 동일 구조(수업 §11, §13, §15)이므로 인코더-디코더 채택
  → 베이스라인: 처음부터 학습하는 인코더-디코더 Transformer (수업 §11 구조 재사용, 데이터 규모에 맞춰 축소)
  → 전이학습: 사전학습 인코더-디코더(KoBART/KE-T5) 파인튜닝 (수업 §13/§15 구조 재사용)
```

- **인코더-디코더 vs 디코더 전용, 왜 인코더-디코더인가**: Q와 A는 역할이 뚜렷이 다른 두 시퀀스다. 인코더가 Q 전체를 한 번에 읽어 압축 표현을 만들고, 디코더가 그 표현에 cross-attention으로 "조건화"하면서 A를 한 토큰씩 생성하는 구조가 이 문제에 직접 대응된다. 디코더 전용 구조(GPT류, 수업 §12)는 Q와 A를 하나의 시퀀스로 이어붙여 다음 토큰 예측만으로 학습하는데, 소스/타겟의 경계를 모델이 데이터만으로 학습해야 해서 우리 정도 규모(1만 건 남짓)의 데이터에는 상대적으로 불리하다고 판단했다.
- **베이스라인 모델을 왜 축소했는가(d_model 256, layer 3)**: 원 논문/수업 예제(d_model 512, layer 6)는 WMT 수준(수백만 문장) 데이터를 가정한 크기다. 우리 데이터(9,400쌍)에 그 크기를 그대로 쓰면 파라미터 대비 데이터가 부족해 과적합·비효율 학습 위험이 크다. 최종 파라미터 6,010,688개.
- **임베딩/출력층 공유(weight tying)**: Q/A가 같은 언어·같은 BPE vocab을 쓰기 때문에 인코더/디코더 임베딩과 출력 projection을 공유해 파라미터를 더 줄였다. 수업 §11의 번역 문제(독일어→영어, 서로 다른 vocab)에서는 쓸 수 없었던 최적화지만, 우리 문제(한국어→한국어)에서는 자연스럽게 적용 가능하다.
- **KoBART/KE-T5를 전이학습 대상으로 고른 이유**: 둘 다 한국어로 사전학습된 인코더-디코더 모델이라 Q→A 생성에 구조적으로 바로 적용 가능하고(수업 §13/§15의 BART/T5 요약 파인튜닝과 동일한 패턴), 특히 KoBART는 한국어 챗봇 응답생성에 공개적으로 널리 검증된 선례가 있다.

---

## 3.5. 실험으로 확인된 것 (03·04번)

- **03번(베이스라인, 30 epoch)**: train/val loss가 29.82→6.3332 / 21.78→6.3724로 계속 감소 중이었는데도 epoch 상한 때문에 멈췄다(조기 종료 걸리지 않음). greedy decoding 결과가 `"마음을 마음을 마음을..."`, `"같이 같이 같이..."`처럼 같은 토큰을 반복하는 붕괴 현상을 보였다. → **고찰**: 원인이 (a) 학습 부족(에폭), (b) greedy decoding의 반복 루프 성질, (c) 데이터 규모의 근본적 한계 중 무엇인지 가설을 세우고, **한꺼번에 건드리면 무엇이 효과가 있었는지 알 수 없으므로** 04번에서 (a)부터 통제 실험으로 검증하기로 함.
- **04번 Part 1 — 베이스라인 재실험(에폭 상한만 30→150, 나머지 완전 동일)**: 117 epoch에서 조기 종료, best val_loss 6.3724→**5.7957**로 개선. 학습곡선을 보면 약 30 epoch 지점부터 train/val loss가 벌어지기 시작(과적합 시작)하지만 val loss는 그 뒤로도 5.8대까지 천천히 더 내려갔다. → **가설 (a) 학습 부족이 주요 원인이었음을 확인**. 다만 val loss가 5.8대에서 평평해지는 것으로 보아 이 데이터·모델 크기 조합의 사실상 성능 한계에 가깝다고 판단(가설 (c)도 부분적으로 유효).
- **04번 Part 2 — KoBART 파인튜닝**: 파라미터 1.24억 개(베이스라인의 약 21배)로, 단 7 epoch(조기 종료, best val_loss **2.7424**)만에 베이스라인이 117 epoch으로 도달한 수준(5.80)을 크게 앞질렀다 — "사전학습 지식 덕분에 적은 데이터로도 빠르게 수렴한다"는 전이학습의 핵심 가설을 수치로 확인. 다만 train loss가 0.44까지 내려가는 동안 val loss는 5 epoch째부터 반등해 **소규모 데이터에 빠르게 과적합**하는 경향도 관찰됨(06번에서 다룸).

---

## 4. 학습 → 디코딩 → 평가 → 오류분석 흐름

```
04 베이스라인 재실험(고찰) + KoBART 파인튜닝
  → 05 디코딩 전략 비교 (greedy/beam/top-k·top-p)
  → 06 하이퍼파라미터 비교 (임베딩 초기화/lr/스케줄, KE-T5 비교)
  → 07 정량 평가 (BLEU/ROUGE-L/Distinct-n/빈 응답 비율) — 모델 9종을 같은 표에서 비교
  → 08 오류 분석 — 왜 그 수치가 나왔는지 실패 사례로 설명
  → (07에서 발견한 이상 신호를 08에서 추적 → 결함 규명 → 04/06 수정 → 전체 재실행)
```

(애초 계획에는 04번이 KoBART 파인튜닝만 포함했지만, 03번 결과의 반복 붕괴 원인을 먼저 검증할 필요가 있어 "베이스라인 재실험" 파트를 04번 앞부분에 추가했다. KE-T5 비교는 06번으로 이월했다.)

- **디코딩 전략을 별도 노트북으로 분리한 이유**: 디코딩 전략은 "어떤 모델을 쓰는가"와 독립적인 축이다. 이 축을 모델 선택 축과 분리해야 "성능 차이가 모델 때문인지 디코딩 방식 때문인지"를 헷갈리지 않고 설명할 수 있다.
- **평가지표를 4개(BLEU/ROUGE-L/Distinct-n/빈 응답 비율) 같이 보는 이유**: BLEU/ROUGE는 정답과의 표면적 유사도만 보기 때문에, 챗봇 응답처럼 "정답이 여러 개 있을 수 있는" 생성 태스크에서는 그것만으로는 오해의 소지가 있다. Distinct-n(생성 다양성)과 빈 응답 비율(생성 실패/붕괴 여부)을 같이 봐야 종합적으로 판단할 수 있다. **이 설계가 실제로 값을 했다** — KE-T5의 모드 붕괴는 corpus 단위 Distinct-1(0.002)로만 드러났고, 평균 길이 지표가 EOS 결함 발견의 첫 단서였다.
- **오류분석을 마지막에 두는 이유**: 07번의 숫자만으로는 "왜" 그런 점수가 나왔는지 알 수 없다. 08번에서 실패 사례를 유형화해서 수치 개선의 실제 내용을 설명한다.

### 4.5. 가장 중요한 발견 — 전이학습 파이프라인의 EOS 누락 결함

**발견 경위**: 07번 최초 평가에서 사전 예상과 정반대 결과가 나왔다. 사전학습 KoBART(BLEU 3.83)가 처음부터 학습한 베이스라인(4.87)에 **졌다**. 04번의 val_loss 순위(KoBART 2.81 vs 베이스라인 5.80)와도, 눈으로 본 생성 품질과도 어긋났다.

**판단**: 정성적 인상과 정량 지표가 어긋날 때 가능한 설명은 (a) 지표가 잘못 재고 있다, (b) 파이프라인에 결함이 있다 둘 중 하나다. "챗봇은 정답이 여러 개니까 지표의 한계겠지"로 넘어가는 것이 쉬운 길이었지만, **(b)부터 검증했고 실제로 결함이 있었다.**

**추적 과정** (08번 5절):

| 단계 | 확인된 사실 |
|---|---|
| 단서 1 | KoBART 응답의 **정상 문장 종결률 32.3%** (베이스라인 99.6%), 평균 길이 12.32형태소 (정답 6.46의 약 2배) |
| 단서 2 | `gogamza/kobart-base-v2` 토크나이저는 `text_target=` 인코딩 시 **EOS를 붙이지 않는다** → 모델이 "문장을 끝내는 법"을 학습하지 못함 |
| 단서 3 (대안 가설 기각) | "타깃이 max_length에서 잘려 EOS 소실" → 실제 잘리는 타깃은 0.8%(77/9,400건)뿐 |
| 근본 원인 | 베이스라인은 03번 `encode_text()`에서 `[BOS] + ids + [EOS]`로 EOS를 직접 붙였음. **같은 처리를 전이학습 쪽에만 빠뜨린 것** |

**수정과 재실행**: 04/06번 collate에 `encode_targets_with_eos()`를 추가해 타깃 끝에 EOS를 붙이고(토크나이저가 이미 붙이는 경우는 제외), **다른 조건은 전부 고정한 채** 04 → 05 → 06 → 07 → 08을 재실행했다.

| 모델 | BLEU 전→후 | 정상 종결% 전→후 | 평균 길이 전→후 |
|---|---|---|---|
| KoBART lr5e-5 | 3.83 → **7.72** | 32.3 → **98.0** | 12.32 → **6.28** |
| KoBART B2 (warmup) | 3.38 → **7.90** | 34.6 → **98.4** | 12.31 → **6.22** |
| KE-T5 *(자연 대조군)* | 0.09 → 0.09 | 100.0 → 100.0 | 4.00 → 4.00 |
| baseline-114ep *(대조군)* | 4.87 → 4.87 | 99.6 → 99.6 | 5.18 → 5.18 |

**대조군 두 개가 전혀 변하지 않았다는 점이 인과를 확정해준다.** 베이스라인은 원래 EOS를 붙이고 있었고, KE-T5는 토크나이저가 EOS를 자동으로 붙여 이번 수정이 no-op였다. 즉 **결함이 있던 모델만 정확히 골라서 개선됐다.**

**부수 효과**: 05번에서 "반복 억제에 `no_repeat_ngram`이 결정적"이라던 최초 결론이 재해석됐다. 그 반복 자체가 EOS 결함의 증상이었고, 원인을 고치자 디코딩 전략과 무관하게 반복이 사라져 이 설정은 사실상 무효(no-op)가 됐다. **증상을 덮는 대신 원인을 찾는 것이 옳은 해법이었다.**

---

## 5. 최종 결과 요약

| 모델 | 계열 | BLEU | ROUGE-L | Distinct-2 | 학습 epoch |
|---|---|---|---|---|---|
| **KoBART B2 (lr=5e-5 + warmup)** | 전이학습 | **7.90** | **25.14** | **0.510** | 7 |
| KoBART (lr=5e-5) | 전이학습 | 7.72 | 24.90 | 0.486 | 7 |
| KoBART B1 (lr=2e-5) | 전이학습 | 6.72 | 24.18 | 0.496 | 7 |
| baseline-114ep | 베이스라인 | 4.87 | 22.99 | 0.303 | 117 |
| A2 (lr=3e-4) | 베이스라인 | 4.87 | 22.78 | 0.239 | 42 |
| baseline-30ep | 베이스라인 | 2.64 | 22.43 | 0.070 | 30 |
| KE-T5 | 전이학습 | 0.09 | 19.47 | 0.003 | 7 (수렴 실패) |

**전이학습이 베이스라인을 BLEU 62%, 다양성 68% 앞섰고, 학습 비용은 오히려 적다**(7 epoch vs 117 epoch). 정확도와 다양성을 동시에 끌어올렸다는 점이 중요하다 — 보통 이 둘은 상충한다.

**주의**: 08번 수동 판정에서 KoBART의 '표면 불일치' 10건 중 8건이 **타당한 대안 응답**으로 확인됐다(예: 정답 "당연히 가능하죠." vs 생성 "가능합니다!" — 의미 동일, 형태소 0% 겹침). 즉 **현재 BLEU 7.90은 과소평가된 값**이며, 사람 평가나 의미 기반 지표가 필요하다.

---

## 6. 최종 산출물과의 연결

- `report/결과보고서.pdf`의 각 장은 특정 노트북의 산출물을 그대로 인용한다 (2장 ↔ 01번, 3장 ↔ 02번, 8장 ↔ 07번, 9장 ↔ 08번).
- `slides/SLIDES_OUTLINE.md`도 동일한 순서를 따른다.
- 즉 노트북 순서 = 보고서/발표자료 목차 순서로 설계되어 있어서, 노트북이 끝나면 발표자료 작성은 결과를 옮겨담는 작업에 가깝다.

---

## 진행 상태 요약

| 단계 | 노트북 | 상태 |
| --- | --- | --- |
| 데이터 전처리 | `01_data_eda.ipynb` | 완료 |
| 토큰화/임베딩 | `02_tokenization_embedding.ipynb` | 완료 |
| 베이스라인 모델 | `03_baseline_transformer.ipynb` | 완료 (30 epoch, 반복 붕괴 관찰) |
| 베이스라인 재실험 + 전이학습 | `04_finetune_pretrained.ipynb` | 완료 (117ep / KoBART 7ep, EOS 결함 수정 반영) |
| 디코딩 전략 | `05_decoding_strategy.ipynb` | 완료 |
| 하이퍼파라미터 비교 | `06_hparam_comparison.ipynb` | 완료 (EOS 결함 수정 반영) |
| 정량 평가 | `07_evaluation.ipynb` | 완료 (모델 9종, EOS 수정 전후 비교 포함) |
| 오류 분석 | `08_error_analysis.ipynb` | 완료 (결함 규명 과정 포함) |
| 결과보고서 | `report/결과보고서.pdf` | 완료 (27페이지) |
| 발표자료 | `slides/` | **예정** |
