# 프로젝트 전체 흐름

이 문서는 "왜 이 순서로, 왜 이런 선택을 했는가"를 하나의 이야기로 잇는 문서다. 각 단계의 세부 실행/근거는 해당 노트북에, 남은 작업 목록은 `PLAN.md`에 있다.

## 0. 과제의 뼈대

과제 요구사항(CLAUDE.md)을 한 문장으로 요약하면: _"토큰화·임베딩·전이학습을 어떻게, 왜 선택했는지 근거를 대고, 베이스라인 대비 무엇이 얼마나 개선됐는지 실험과 오류분석으로 증명하라"_ 이다. 그래서 이 프로젝트는 처음부터 하나의 대비 구조로 설계했다:

> **베이스라인** = 사전학습 없이 처음부터 학습하는 Transformer
> **고도화(전이학습)** = 사전학습된 KoBART/KE-T5를 파인튜닝
>
> 이 둘을 **동일한 데이터, 동일한 평가 지표**로 비교해서 "전이학습이 실제로 무엇을 얼마나 개선하는가"를 수치와 오류 사례로 보여주는 것이 프로젝트의 핵심 줄거리다.

이 줄거리를 따라가면 각 노트북이 왜 그 순서로 존재하는지 자연스럽게 설명된다.

---

## 1. 데이터 전처리 흐름 (`01_data_eda.ipynb`)

```
원본 로드 → 구조 확인(결측치) → 중복 확인(직접 행 확인) → 정제(공백 정규화)
→ 길이 분포 확인(percentile) → train/val/test 분할(label 비율 유지) → 저장
```

- **왜 EDA가 가장 먼저인가**: 뒤 단계(토크나이저 vocab size, 모델 max_length, 평가 시 label별 성능 해석)에서 쓸 "근거 숫자"를 여기서 확보해야 하기 때문이다. 예를 들어 Q/A 길이의 95 percentile을 몰랐다면 03번 노트북의 `MAX_LEN`을 감으로 정했을 것이다.
- **중복 제거를 (Q, A) 쌍 기준으로 한 이유**: Q만 같고 A가 다른 경우는 "같은 질문에 여러 답이 있을 수 있다"는 자연스러운 다양성이라 남겨야 하지만, (Q, A)가 완전히 같은 행은 정보량 없이 특정 패턴의 손실 가중치만 키운다.
  - (사용자 메모, 확인 예정) 반대로 (Q, A)는 같은데 label만 다른 행이 있다는 점도 발견했다. 이건 "중복이라 지워도 되는가"의 경계 사례라 `PLAN.md`의 "나중에 추가로 확인할 사항"에 남겨두고, 결론 나는 대로 이 문서와 01번 노트북에 반영한다.
- **95 percentile을 max_length 기준으로 쓰는 이유**: 최댓값 기준으로 잡으면 극소수 이상치 문장 때문에 대다수의 짧은 문장이 불필요하게 padding되어 연산이 낭비된다. 95 percentile은 "이상치 5%의 일부 잘림을 감수하고 나머지 95%는 손실 없이 담는" 절충점이다. 이 기준은 03번 노트북에서 실제 `MAX_LEN`을 정할 때도 (이번엔 BPE 토큰 길이 기준으로) 그대로 재사용한다 — 즉 01번에서 세운 원칙이 이후 노트북 설계에 계속 적용된다.
- **산출물**: `dataset/processed/{train,val,test}.csv` — 이후 모든 노트북의 공통 입력.

---

## 2. 분석 단위(토큰화) 선정 흐름 (`02_tokenization_embedding.ipynb`)

```
어절/음절/형태소/자모 비교 (정성+정량)
  → "형태소가 의미 보존과 시퀀스 길이의 절충점"이라는 결론
  → 그러나 모델 입력으로는 형태소 분석만으로 부족(신조어/오타에 약함, OOV)
  → 서브워드(SentencePiece BPE) 직접 학습으로 해결
  → 사전학습 모델(KoBART) 토크나이저와 비교
  → "베이스라인엔 직접 학습한 BPE, 전이학습 모델엔 그 모델 고유 토크나이저" 라는 분기 확정
  → Word2Vec(gensim) 임베딩 학습 및 코사인 유사도로 품질 검증
```

- **왜 네 가지 단위를 다 비교했는가**: "형태소 단위를 쓴다"는 결론이 감이 아니라 실측(평균 토큰 수: 어절 3.5 < 형태소 5.3 < 음절 10.3 < 자모 24.5)과, 실제 신조어 예시("갓생살기")에서 형태소 분석기가 깨지는 것을 눈으로 확인한 근거 위에 서 있어야 하기 때문이다.
- **BPE를 벼로 도입한 이유**: 형태소 분석기는 규칙/사전 기반이라 학습 데이터에 없던 표현에 약하다. BPE는 통계적 병합이라 어떤 문자열이든 subword 조합으로 표현할 수 있어 OOV가 사실상 사라진다 — 이건 우리가 다음 단계에서 쓸 모든 사전학습 Transformer(BERT/GPT/BART/T5, KoBART 포함)가 공통적으로 서브워드를 쓰는 이유와 같다.
- **"왜 두 개의 토크나이저(자체 BPE와 KoBART 토크나이저)를 병행하는가"**: 이게 이 단계에서 가장 중요한 분기점이다. 사전학습 모델의 임베딩 행렬은 그 모델 고유의 vocab index에 맞춰 학습되어 있어서, 파인튜닝 시 다른 토크나이저를 쓰면 그 사전학습 지식 자체가 무의미해진다. 그래서:
  - **베이스라인(03번)** → 우리가 직접 학습한 `chatbot_bpe.model` 사용
  - **전이학습(04번)** → KoBART/KE-T5 각각의 `AutoTokenizer`를 그대로 사용
  - 이 분기가 03번과 04번 노트북의 데이터 파이프라인이 서로 다른 근본 이유다.
- **Word2Vec을 별도로 학습한 이유**: 토크나이저 선택과는 별개로, "임베딩이 학습되면서 의미 공간이 어떻게 형성되는가"를 단계별(형태소분석→vocab구축→중심-주변 단어 쌍 생성→skip-gram 학습→유사도 확인)로 직접 보여주기 위함이다. 이 임베딩 자체는 04번(KoBART)에는 쓰이지 않지만(사전학습 모델은 자체 임베딩을 쓰므로), 03번 베이스라인의 임베딩 초기화 방식(랜덤 vs Word2Vec)을 비교하는 실험(06번 노트북)의 재료로 이월된다.
- **산출물**: `dataset/processed/chatbot_bpe.model`(+`.vocab`), `dataset/processed/word2vec.model` — 03번(BPE)과 06번(Word2Vec)에서 재사용.

---

## 3. 모델 구조 선정 흐름

```
"Q→A는 조건부 생성 문제" 라는 문제 정의
  → 기계번역/요약과 동일 구조(수업 §11, §13, §15)이므로 인코더-디코더 채택
  → 베이스라인: 처음부터 학습하는 인코더-디코더 Transformer (수업 §11 구조 재사용, 데이터 규모에 맞춰 축소)
  → 전이학습: 사전학습 인코더-디코더(KoBART/KE-T5) 파인튜닝 (수업 §13/§15 구조 재사용)
```

- **인코더-디코더 vs 디코더 전용, 왜 인코더-디코더인가**: Q와 A는 역할이 뚜렷이 다른 두 시퀀스다. 인코더가 Q 전체를 한 번에 읽어 압축 표현을 만들고, 디코더가 그 표현에 cross-attention으로 "조건화"하면서 A를 한 토큰씩 생성하는 구조가 이 문제에 직접 대응된다. 디코더 전용 구조(GPT류, 수업 §12)는 Q와 A를 하나의 시퀀스로 이어붙여 다음 토큰 예측만으로 학습하는데, 소스/타겟의 경계를 모델이 데이터만으로 학습해야 해서 우리 정도 규모(1만 건 남짓)의 데이터에는 상대적으로 불리하다고 판단했다. (참고: `dataset/processed/`의 데이터로 이 판단 자체를 검증해보는 실험을 여유가 되면 추가 비교군으로 넣을 수 있다.)
- **베이스라인 모델을 왜 축소했는가(d_model 256, layer 3)**: 원 논문/수업 예제(d_model 512, layer 6)는 WMT 수준(수백만 문장) 데이터를 가정한 크기다. 우리 데이터(9,400쌍)에 그 크기를 그대로 쓰면 파라미터 대비 데이터가 부족해 과적합·비효율 학습 위험이 크다. 그래서 데이터 규모에 맞춰 절반 크기로 줄였다.
- **임베딩/출력층 공유(weight tying)**: Q/A가 같은 언어·같은 BPE vocab을 쓰기 때문에 인코더/디코더 임베딩과 출력 projection을 공유해 파라미터를 더 줄였다. 수업 §11의 번역 문제(독일어→영어, 서로 다른 vocab)에서는 쓸 수 없었던 최적화지만, 우리 문제(한국어→한국어)에서는 자연스럽게 적용 가능하다.
- **KoBART/KE-T5를 전이학습 대상으로 고른 이유**: 둘 다 한국어로 사전학습된 인코더-디코더 모델이라 Q→A 생성에 구조적으로 바로 적용 가능하고(수업 §13/§15의 BART/T5 요약 파인튜닝과 동일한 패턴), 특히 KoBART는 한국어 챗봇 응답생성에 공개적으로 널리 검증된 선례가 있다.

---

## 3.5. 실험으로 확인된 것 (03·04번 노트북 실행 결과)

앞의 "근거"들이 실제로 맞았는지를 03/04번 노트북 실행 결과로 검증했다.

- **03번(베이스라인, 30 epoch)**: train/val loss가 29.75→6.34 / 21.89→6.38로 계속 감소 중이었는데도 epoch 상한 때문에 멈췄고, greedy decoding 결과가 `"사람 사람 사람 사람..."`처럼 같은 토큰을 반복하는 붕괴 현상을 보였다. → **고찰**: 원인이 (a) 학습 부족(에폭), (b) greedy decoding의 반복 루프 성질, (c) 데이터 규모의 근본적 한계 중 무엇인지 가설을 세우고, 04번에서 (a)부터 통제 실험으로 검증하기로 함.
- **04번 Part 1 — 베이스라인 재실험(에폭 상한만 30→150, 나머지 완전 동일)**: 114 epoch에서 조기 종료(patience=10), train/val loss가 6.34→3.36 / 6.38→5.83으로 개선. 학습곡선을 보면 **약 30 epoch 지점부터 train/val loss가 벌어지기 시작**(과적합 시작)하지만 val loss는 그 뒤로도 5.8대까지 천천히 더 내려갔다. 생성 결과에서 반복 붕괴가 사라지고 `"베터리 겨우 15%야"→"미리 충전하세요."`처럼 정답과 정확히 일치하는 사례도 나옴. → **가설 (a) 학습 부족이 주요 원인이었음을 확인**. 다만 val loss가 5.8대에서 평평해지는 것으로 보아 이 데이터·모델 크기 조합의 사실상 성능 한계에 가깝다고 판단.
- **04번 Part 2 — KoBART 파인튜닝**: 파라미터 수 1.24억 개(베이스라인의 약 21배)로, 단 7 epoch(조기 종료, best val_loss 2.79)만에 베이스라인보다 훨씬 낮은 loss에 도달 — "사전학습 지식 덕분에 적은 데이터로도 빠르게 수렴한다"는 전이학습의 핵심 가설을 수치로 확인했다. 생성 문장은 베이스라인보다 유창하고 표현이 풍부해졌지만(`"쓸데 없는 걱정일 수도 있어요. 어서 충전하세요."`), 일부 응답에서 greedy decoding 특유의 반복(`"그럴 거예요. 그럴 거예요."`)이 여전히 남아있고, train loss가 0.42까지 내려가는 동안 val loss는 4 epoch째부터 반등해 **소규모 데이터에 빠르게 과적합**하는 경향도 함께 관찰됨.
- **이어지는 질문들**: (1) greedy 특유의 반복 문제는 05번(디코딩 전략: beam/sampling)에서 완화되는지, (2) KoBART의 과적합 경향은 06번(하이퍼파라미터 비교: lr/epoch/early stopping 기준)에서 어떻게 다룰지, (3) "베이스라인 vs KoBART" 대비가 실제 수치(BLEU/ROUGE/Distinct-n)로도 확인되는지는 07번에서 다룬다.

---

## 4. 학습 → 디코딩 → 평가 → 오류분석 흐름

```
04 베이스라인 재실험(고찰) + KoBART 파인튜닝 [완료]
  → 05 디코딩 전략 비교 (greedy/beam/top-k·top-p) — 베이스라인과 파인튜닝 모델 모두에 적용
  → 06 하이퍼파라미터 비교 (lr/batch/embedding 초기화, KoBART 과적합 억제 등)
  → 07 정량 평가 (BLEU/ROUGE-L/Distinct-n/빈 응답 비율) — baseline(30ep/150ep) vs KoBART를 같은 표에서 비교
  → 08 오류 분석 — 왜 그 수치가 나왔는지 실패 사례로 설명, 베이스라인→개선판 사이 오류 유형 변화 추적
```

(애초 계획에는 04번이 KoBART 파인튜닝만 포함했지만, 03번 결과의 반복 붕괴 원인을 먼저 검증할 필요가 있어 "베이스라인 재실험" 파트를 04번 앞부분에 추가했다. KE-T5 비교는 06번 하이퍼파라미터/모델 비교 노트북으로 이월했다.)

- **디코딩 전략을 별도 노트북으로 분리한 이유**: 디코딩 전략은 "어떤 모델을 쓰는가"와 독립적인 축이다. 같은 파인튜닝된 모델도 greedy는 안전하지만 뻔한 답을, sampling은 다양하지만 불안정한 답을 낼 수 있다. 이 축을 모델 선택 축과 분리해야 "성능 차이가 모델 때문인지 디코딩 방식 때문인지"를 헷갈리지 않고 설명할 수 있다. 04번에서 이미 KoBART의 greedy 결과에 반복 문제가 관찰되었으므로, 05번은 "이 반복이 디코딩 전략을 바꾸면 실제로 줄어드는가"라는 구체적 질문에서 출발한다.
- **평가지표를 4개(BLEU/ROUGE-L/Distinct-n/빈 응답 비율) 같이 보는 이유**: BLEU/ROUGE는 정답과의 표면적 유사도만 보기 때문에, 챗봇 응답처럼 "정답이 여러 개 있을 수 있는" 생성 태스크에서는 그것만으로는 오해의 소지가 있다. Distinct-n(생성 다양성)과 빈 응답 비율(생성 실패/붕괴 여부)을 같이 봐야 "베이스라인이 다양성 없는 뻔한 답만 반복하는지, 파인튜닝 모델이 정말 더 나은지"를 종합적으로 판단할 수 있다.
- **오류분석을 마지막에 두는 이유**: 07번의 숫자만으로는 "왜" 그런 점수가 나왔는지 알 수 없다. 08번에서 baseline과 개선판 각각의 실패 사례를 유형화(예: 문맥 무시, 동어반복, 라벨(감정)과 안 맞는 응답 등)해서, 수치 개선의 실제 내용을 설명한다. 04번에서 이미 관찰한 "KoBART의 반복 패턴", "베이스라인의 짧고 단조로운 응답" 같은 정성적 관찰이 08번 오류 유형 분류의 출발점이 된다.

---

## 5. 최종 산출물과의 연결

- `report/REPORT_TEMPLATE.md`의 각 절은 특정 노트북의 산출물을 그대로 인용하도록 이미 맞춰져 있다 (예: "2. 데이터"절 ↔ 01번, "3. 토큰화 및 임베딩" ↔ 02번, "7. 평가" 표 ↔ 07번).
- `slides/SLIDES_OUTLINE.md`도 동일한 순서를 따른다.
- 즉 노트북 순서 = 보고서/발표자료 목차 순서로 설계되어 있어서, 노트북이 끝나면 보고서/발표자료 작성은 결과를 옮겨담는 작업에 가깝다.

---

## 진행 상태 요약

| 단계                                  | 노트북                            | 상태                                                                   |
| ------------------------------------- | --------------------------------- | ---------------------------------------------------------------------- |
| 데이터 전처리                         | `01_data_eda.ipynb`               | 완료 (사용자 확인 완료, 메모 1건 보류)                                 |
| 토큰화/임베딩                         | `02_tokenization_embedding.ipynb` | 완료 (사용자 확인 완료)                                                |
| 베이스라인 모델                       | `03_baseline_transformer.ipynb`   | 완료 (사용자 확인 완료 — 30 epoch, greedy 반복 붕괴 관찰)              |
| 베이스라인 재실험 + 전이학습 파인튜닝 | `04_finetune_pretrained.ipynb`    | 완료 (사용자 확인 완료 — 베이스라인 114ep 재학습, KoBART 7ep 파인튜닝) |
| 디코딩 전략                           | `05_decoding_strategy.ipynb`      | 다음 단계                                                              |
| 하이퍼파라미터 비교                   | `06_hparam_comparison.ipynb`      | 예정                                                                   |
| 정량 평가                             | `07_evaluation.ipynb`             | 예정                                                                   |
| 오류 분석                             | `08_error_analysis.ipynb`         | 예정                                                                   |
| 보고서/발표자료                       | `report/`, `slides/`              | 예정                                                                   |
