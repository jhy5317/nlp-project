# final.ipynb 전체 흐름

**작성일**: 2026-09-21
**대상**: `notebooks/final.ipynb` (통합 노트북, 140셀 / 코드 86셀)
**실행 결과**: `results/final/`, `checkpoints/final/`
**보고서**: `report/최종결과보고서.pdf` (24페이지)

이 문서는 `final.ipynb` **한 파일만**을 대상으로 "왜 이 순서로, 왜 이런 선택을 했는가"를 하나의 이야기로 잇는다.

> **기존 문서와의 관계**
> `PROJECT_FLOW.md`와 `PLAN.md`는 노트북 `01~08`번을 나눠 진행하던 과정과 그 실행 결과(2026-09-19)를 서술하며, 이 문서와 별개로 **그대로 보존**한다.
> `final.ipynb`는 같은 설계를 하나의 실행 흐름으로 재구성해 2026-09-21에 처음부터 다시 돌린 것이다. 따라서 수치가 일부 다르다 — 차이의 원인은 [9장](#9-0108번-실행과-수치가-다른-이유)에 정리했다.

---

## 0. 설계 원칙 — 한 번에 하나의 축만 바꾼다

성능이 달라졌을 때 "무엇 때문에 달라졌는가"를 말할 수 있으려면, 비교하는 두 조건 사이에 다른 점이 하나뿐이어야 한다. 8단계 배치는 전적으로 이 원칙을 따른다.

| 단계 | 바꾸는 축 | 고정하는 것 |
| --- | --- | --- |
| 3 → 4 | 모델 (처음부터 학습 → 사전학습 파인튜닝) | 데이터 · 분할 · 평가 기준 |
| 5 | (비교만 수행) | 디코딩을 greedy로 통일 → **모델 축만** 본다 |
| 6 | 디코딩 전략 | 모델 가중치 고정 → **디코딩 축만** 본다 |
| 7 | 파인튜닝 하이퍼파라미터 | 모델 종류 · 데이터 · 디코딩 · 학습 예산 |
| 8 | (종합 평가) | 6단계에서 고른 디코딩으로 전 모델 통일 |

**5단계에서 굳이 greedy를 쓰는 이유**: 그 시점에는 디코딩 전략을 아직 고르지 않았다. beam이나 sampling을 쓰면 "베이스라인과 전이학습의 차이"에 "디코딩의 효과"가 섞여 들어간다. greedy는 매 스텝 확률 1등만 고르는 가장 단순하고 결정적인 방식이라, 두 모델을 **모델 자체의 성능만으로** 비교하는 중립 기준선이 된다.

**전체 실행 시간**: 24분 (RTX 4070 Ti 12GB, 전 단계 처음부터 재학습)

---

## 1. 데이터 전처리

```
원본 로드 → 구조/결측 확인 → 중복 3기준 측정 → 정제 → 길이 분포 측정
→ train/val/test 분할(label stratify) → 저장
```

**왜 가장 먼저인가**: 뒤 단계에서 쓸 "근거 숫자"를 여기서 확보해야 한다. 토크나이저의 `vocab_size`, 모델의 `MAX_LEN`, 8단계 label별 성능 해석 — 전부 데이터의 실제 분포를 알아야 정할 수 있다. Q/A 길이 분포를 모르면 `MAX_LEN`을 감으로 정하게 된다.

**중복 제거를 (Q, A) 쌍 기준으로 한 근거**: Q만 같고 A가 다른 행은 "같은 질문에 여러 답이 있을 수 있다"는 자연스러운 다양성이라 보존해야 한다. (Q, A)가 완전히 같은 행은 정보량을 더하지 않으면서 특정 패턴의 손실 가중치만 키워 모델을 편향시킨다.

**95 percentile을 max_length 기준으로 쓰는 근거**: 최댓값으로 잡으면 이상치 몇 건 때문에 대다수의 짧은 문장이 불필요하게 padding되어 연산이 낭비된다. 평균으로 잡으면 긴 문장이 많이 잘린다. 95p는 "상위 5%의 일부 잘림을 감수하고 95%는 손실 없이 담는" 절충점이다.

**실측**

| 항목 | 값 |
| --- | --- |
| 원본 → 정제 | 11,823건 → **11,750건** (73건 제거) |
| label 분포 | 일상 5,290 / 이별 3,570 / 사랑 2,963 |
| Q / A 길이 95p | 25음절 / 27음절 |
| 분할 | train **9,400** / val **1,175** / test **1,175** |

**다음으로 넘기는 것**: 길이 95p(2단계에서 BPE 토큰 단위로 재측정), label 분포(8단계 해석 근거).

---

## 2. 토큰화 · 임베딩 · 인코딩 전략

```
토큰화 단위 4종 비교(어절/음절/형태소/자모)
  → "형태소가 의미 보존과 시퀀스 길이의 절충점"
  → 그러나 모델 입력으로는 부족(신조어/오타에 약함)
  → 서브워드(SentencePiece BPE) 직접 학습
  → 사전학습(KoBART) 토크나이저와 비교 → 토크나이저 병행 분기 확정
  → Word2Vec 임베딩 5단계 학습 및 유사도 검증
  → 인코딩 전략 확정(BOS/EOS, MAX_LEN, 동적 패딩)
```

### 2.1 토큰화 단위 — 왜 넷을 다 재봤는가

"형태소를 쓴다"는 결론이 감이 아니라 실측 위에 서 있어야 하기 때문이다.

| 단위 | 평균 토큰 수 | 상대 연산량(길이²) |
| --- | --- | --- |
| 어절 | 3.53 | 1.0 |
| 형태소(Okt) | 5.26 | 2.2 |
| 음절 | 10.31 | 8.5 |
| 자모 | 24.54 | **48.3** |

self-attention 비용은 시퀀스 길이의 제곱에 비례한다. **무조건 잘게 쪼갠다고 좋은 것이 아니다** — 자모 단위는 어절 대비 약 48배의 연산을 요구한다. 형태소가 절충점이며, 이 결론을 Word2Vec 학습(2.4)과 평가 지표 계산(5단계)에 그대로 적용한다.

### 2.2 왜 서브워드(BPE)인가

- **형태소 분석만으로 부족한 이유**: Okt는 사전(rule) 기반이라 미등록어에 약하다. 실측에서 `"갓생살기"`를 `갓 / 생 / 살기`로, `"월루했다"`를 통째로 분석했다. 챗봇은 사용자 입력을 통제할 수 없어 이 약점이 치명적이다.
- **BPE가 해결하는 것**: 사전이 아니라 문자 병합 통계로 vocab을 만들어 처음 보는 문자열도 subword 조합으로 표현한다. `"오늘 월루했다"`의 unknown 토큰이 **0개**였다. (`"ㄹㅇㅋㅋ 완전 갓생살기 아님?"`은 자모 나열 부분에서 2개 발생 — 학습 코퍼스에 없던 자모 조합까지 완벽하지는 않다는 점도 함께 확인했다.)
- **WordPiece가 아닌 SentencePiece**: 공백까지 포함한 raw text를 다뤄 띄어쓰기가 불규칙한 구어체 한국어에 안정적이고, 4단계에서 쓸 KoBART와 방식이 일관된다.
- **`vocab_size=8000` 근거**: 코퍼스가 짧은 대화문 약 1.9만 줄(9,400쌍 × 2)로 크지 않다. 3만 이상이면 저빈도 subword가 많아 비효율적이고, 1천 이하면 지나치게 잘게 쪼개져 의미 단위가 깨진다.

### 2.3 왜 토크나이저를 두 개 쓰는가 — 이 단계의 핵심 분기

사전학습 모델의 임베딩 행렬은 **그 모델 토크나이저가 부여한 vocab index에 1:1로 대응**한다. 직접 학습한 BPE로 바꿔 넣으면 같은 index가 전혀 다른 subword를 가리켜 사전학습 지식이 통째로 무의미해진다 — 전이학습을 한다면서 전이할 것을 버리는 셈이다.

| 모델 | 토크나이저 | 근거 |
| --- | --- | --- |
| 베이스라인 (3단계) | 직접 학습한 `chatbot_bpe` (vocab 8,000) | 사전학습 가중치가 없으므로 데이터에 최적화된 vocab이 유리 |
| 전이학습 (4·7단계) | KoBART `AutoTokenizer` 그대로 | 사전학습 임베딩이 해당 토크나이저 index에 종속 |

**이 분기가 3단계와 4단계의 데이터 파이프라인이 서로 다른 근본 이유다.**

### 2.4 임베딩 — 단계별로 드러내기

3단계 Transformer는 임베딩을 모델 안에서 같이 학습하므로 "의미 공간이 어떻게 형성되는지" 관찰할 수 없다. 그래서 Word2Vec을 별도로 학습해 과정을 5단계로 드러냈다.

```
1. 형태소(Okt) 분석으로 문장을 토큰 리스트로 변환
2. vocab 구축 (min_count=2 미만 제외)
3. 중심 단어마다 window(5) 내 (중심, 주변) 쌍 생성
4. Skip-gram + negative sampling으로 벡터 학습
5. 코사인 유사도로 의미 공간 검증 → PCA 시각화
```

- **skip-gram(sg=1)**: CBOW는 고빈도 단어에 유리한데, 챗봇 대화는 구어체 표현이 다양하고 저빈도 단어가 많다. skip-gram은 저빈도 단어도 학습 기회를 여러 번 얻는다.
- **vector_size=100**: 코퍼스가 약 1.9만 문장으로 작아 300차원은 과적합·희소성 위험이 있다.
- **workers=1**: gensim 기본값(3)은 멀티스레드라 seed를 고정해도 실행마다 벡터가 달라진다. 재현성을 위해 단일 스레드로 고정했다.
- **입력을 BPE가 아닌 형태소로**: subword는 사람이 읽었을 때 의미 단위가 아닌 경우가 많아, 해석 가능성이 목적인 관찰에는 형태소가 적합하다(2.1의 결론과 같은 이유).

**실측**: 학습 단어 **6,048개**. 유사도가 의미대로 형성됐다 — `사랑` → 인정(.778) / 존중(.733) / 노력(.729), `이별` → 폭풍(.832) / 환승(.802) / 재회(.797), `친구` → 오빠(.832) / 가족(.827).

### 2.5 인코딩 전략

| 항목 | 결정 | 근거 |
| --- | --- | --- |
| 특수 토큰 | `[BOS] + ids + [EOS]` | BOS는 생성 시작점, **EOS는 문장 종결 신호**. EOS가 없으면 모델은 문장을 끝내는 법을 배우지 못한다 |
| `MAX_LEN` | **16** (BPE 95p 10 + BOS/EOS 2 → 8의 배수) | 1단계 percentile 원칙을 실제 입력 단위로 재적용 |
| 패딩 | 배치 단위 동적 패딩 | 고정 패딩은 짧은 문장이 모인 배치에서 연산 낭비 |
| 손실 제외 | `ignore_index=PAD_ID` | padding 위치 예측은 무의미하며 포함 시 학습이 왜곡 |

> **여기서 붙인 EOS를 기억할 것.** 4단계에서 이 처리를 전이학습 쪽에만 빠뜨리면 무슨 일이 벌어지는지가 이 프로젝트의 핵심 발견이다.

---

## 3. 베이스라인 — 처음부터 학습하는 인코더-디코더 Transformer

### 3.1 구조 선택 — 세 후보를 모두 검토한 뒤 고름

문제 정의가 먼저다. **Q→A는 조건부 생성(conditional generation)** 문제다. 입력과 출력이 서로 다른 역할을 가진 두 시퀀스이고, 출력은 한 토큰씩 순차 생성해야 한다.

| 구조 | 대표 모델 | 사전학습 방식 | 적용 가능성 |
| --- | --- | --- | --- |
| 인코더 전용 | BERT, **ELECTRA** | 마스킹 복원 / 치환 토큰 판별 | **불가** |
| 디코더 전용 | GPT, KoGPT2 | 다음 토큰 예측 | 가능하나 불리 |
| **인코더-디코더** | BART, T5, KoBART | 손상된 문장 복원(denoising) | **채택** |

- **ELECTRA를 쓰지 않는 이유**: ELECTRA는 "이 토큰이 원래 것인가, 생성기가 바꿔치기한 것인가"를 **판별**하도록 사전학습된 모델이다. 출력이 위치별 판별 결과일 뿐 **새 토큰을 순차 생성하는 디코더가 아예 없어**, 자유 형식 문장을 만드는 이 과제에 구조적으로 적용할 수 없다. 디코더를 새로 붙일 수는 있으나 그 디코더는 사전학습된 것이 아니므로 전이학습의 이점이 인코더 쪽에만 남는다.
- **디코더 전용을 쓰지 않는 이유**: Q와 A를 한 시퀀스로 이어붙이면 소스/타깃 경계를 모델이 데이터만으로 학습해야 한다. 9,400쌍으로는 그 규칙까지 안정적으로 배우기 부족하다고 판단했다. 또 Q를 단방향으로만 읽어 조건 표현이 약하다.
- **인코더-디코더를 고르는 이유**: 인코더가 Q 전체를 양방향으로 읽어 압축 표현을 만들고, 디코더가 cross-attention으로 조건화하며 A를 생성한다. 문제 정의에 직접 대응한다. **덧붙여, 4단계에서 파인튜닝할 KoBART도 인코더-디코더라 구조를 일치시켜야 "차이가 사전학습 때문인지 구조 때문인지"를 구분할 수 있다.**

### 3.2 크기와 하이퍼파라미터

원 논문(d_model 512, layer 6/6)은 WMT급 수백만 문장을 가정한 크기다. 9,400쌍에 그대로 쓰면 파라미터 대비 데이터가 부족해 과적합·비효율 학습 위험이 크다. **절반으로 축소**했다(d_model 256, layer 3/3, ffn 512, nhead 8).

**weight tying**: Q/A가 같은 한국어·같은 BPE vocab을 쓰므로 인코더/디코더 임베딩과 출력 projection을 모두 공유했다. 서로 다른 언어를 다루는 번역 과제에서는 쓸 수 없는 최적화다.

**early stopping(patience=10, 상한 150)**: 상한이 낮으면 "수렴 전에 예산 소진으로 멈춘 것"과 "과적합이 시작되어 멈춘 것"을 구분할 수 없다. 상한을 충분히 크게 두고 early stopping이 판단하게 하면 학습이 끝난 이유가 명확해진다.

**epoch 30 스냅샷을 따로 저장한 이유**: 8단계 개선 추적에서 "학습량만 바꿨을 때"의 통제 비교군이 필요하다. 같은 학습 과정의 중간 시점을 저장하면 **추가 학습 비용 없이** 모델 구조·데이터·시드·옵티마이저가 전부 동일하고 학습량만 다른 완벽한 비교군을 얻는다.

**실측**

| 항목 | 값 |
| --- | --- |
| 학습 가능 파라미터 | **6,010,688개** |
| 학습 | **117 epoch** (조기 종료, 최적 시점 epoch 107), 4.1분 |
| best val_loss | **5.7957** |
| epoch 30 시점 val_loss | 6.385 (학습량 비교군) |

---

## 4. 전이학습 — KoBART 파인튜닝, 그리고 결함 규명

### 4.1 모델 선정

| 조건 | 요구사항 | KoBART |
| --- | --- | --- |
| 구조 | 3단계에서 고른 인코더-디코더와 일치 | BART = 인코더-디코더 ✔ |
| 언어 | 한국어 사전학습 | ✔ |
| 사전학습 목적 | 생성 과제에 바로 이어붙일 수 있어야 함 | denoising = 생성형 ✔ |
| 접근성 | 재현 가능 | `from_pretrained` 한 줄 ✔ |

`gogamza/kobart-base-v2`, **123,859,968 파라미터**(베이스라인의 약 21배). BART의 사전학습 방식(노이즈를 가한 문장의 원문 복원)은 "입력 시퀀스를 조건으로 출력 시퀀스를 생성"하는 형태 자체를 이미 학습한 것이라, 우리 과제와 **형태가 같아** 구조 변경 없이 파인튜닝이 이어진다.

### 4.2 파이프라인 설계 6단계

| 단계 | 하는 일 | 확인할 것 |
| --- | --- | --- |
| 1 | 모델·토크나이저 다운로드 | 파라미터 수, 인코더-디코더 구조인지 |
| 2 | 모델 전용 토크나이저로 인코딩 | 다른 토크나이저를 쓰면 사전학습 지식이 무의미(2.3) |
| 3 | `max_length` 재산정 | 토크나이저가 다르면 토큰 수도 다르다 → percentile 원칙 재적용 (**KOBART_MAX_LEN = 16**) |
| 4 | **타깃(label) 인코딩** | padding을 `-100`으로, 그리고 **문장 끝에 EOS가 있는지** |
| 5 | 옵티마이저 / lr | 사전학습 가중치를 망가뜨리지 않을 만큼 작게 (AdamW, **5e-5**) |
| 6 | 학습 + early stopping | 소규모 데이터 과적합 감시 (상한 10 / patience 3) |

**lr을 베이스라인(1e-4)보다 작게 잡는 이유**: 사전학습 가중치는 이미 좋은 출발점이라, 큰 lr로 갱신하면 사전학습 지식이 손상되는 catastrophic forgetting이 일어난다.

**fp16이 아니라 bf16을 쓰는 이유**: bf16은 fp32와 지수 범위가 같아 오버플로우 위험이 없고 GradScaler 없이 안정적이다(fp16은 T5 계열에서 NaN 발산이 알려져 있다).

### 4.3 발견 — val_loss는 좋은데 생성이 이상하다

흔히 쓰는 파인튜닝 예제 방식(토크나이저의 `text_target=` 결과를 그대로 label로 사용)으로 먼저 학습했다.

- **val_loss는 베이스라인보다 압도적으로 낮았다** (3.0183 vs 5.7957) — 전이학습 가설대로였다.
- **그런데 생성 문장이 끝나지 않았다.** 여러 문장이 이어지다 중간에서 끊겼다.

> loss가 좋은데 생성이 나쁜 것은 넘어갈 신호가 아니다. val_loss는 teacher forcing 상태, 즉 "앞 토큰이 전부 정답일 때"의 값이고 실제 생성은 자기가 만든 토큰을 다시 입력으로 쓴다(exposure bias). 이 둘이 크게 어긋나면 생성 과정 자체에 문제가 있다는 뜻이다.
>
> 원인 후보는 **(a) 챗봇은 원래 정답이 여러 개라 그렇다**와 **(b) 파이프라인에 결함이 있다**이다. (a)로 넘어가는 것이 쉬운 길이지만 그러면 진짜 문제를 놓친다. **(b)부터 검증했다.**

### 4.4 추적 과정

| 단계 | 확인한 것 | 결과 |
| --- | --- | --- |
| 단서 1 | 생성 길이와 정상 문장 종결률 측정 (test 200건 표본, greedy) | 정상 종결률 **19.0%**, 평균 길이 **31.85음절**(정답 15.13의 2배) → "정답이 여러 개라서"로 설명되지 않는다 |
| 단서 2 | 파인튜닝 타깃 토큰을 직접 출력 | **KoBART 토크나이저는 `text_target=` 인코딩 시 EOS를 붙이지 않는다** (입력 쪽은 붙인다) |
| 단서 3 | "타깃이 잘려 EOS를 잃었다"는 대안 가설 검증 | 잘리는 타깃은 **77건 / 9,400건 (0.8%)** → 대다수의 미완결 응답을 설명할 수 없다. **가설 기각** |

| 항목 | 규명된 내용 |
| --- | --- |
| 직접 원인 | 모델이 EOS를 생성하지 않아 `max_length`까지 계속 생성 |
| **근본 원인** | 타깃에 EOS가 없어 모델이 "문장을 끝내는 법"을 학습할 기회가 아예 없었다 |
| 왜 베이스라인은 멀쩡했나 | 2.5절 `encode_text()`가 `[BOS] + ids + [EOS]`로 EOS를 **직접** 붙였기 때문. **같은 처리를 전이학습 쪽에만 빠뜨린 것** |

### 4.5 수정과 통제 실험 결과

**바꾼 것은 타깃 인코딩 한 줄뿐**이다(없을 때만 EOS 추가). 모델·lr·배치·epoch 상한·시드·데이터는 전부 그대로 두었으므로, 이 재학습은 **"EOS 하나만 바꿨을 때 무엇이 달라지는가"에 대한 통제 실험**이다.

| 구분 | 평균 길이(음절) | 정상 종결률 | best val_loss | 학습 |
| --- | --- | --- | --- | --- |
| 정답(test A) | 15.13 | 99.5% | — | — |
| 수정 전 | 31.85 | **19.0%** | 3.0183 | 8 epoch (최적 5) |
| **수정 후** | **14.86** | **97.5%** | **2.7359** | 7 epoch (최적 4) |
| 베이스라인(대조군) | 15.48 | 95.0% | 5.7957 | 117 epoch |

> **방법론적 교훈**: 정량 지표(loss)와 정성적 관찰(생성문)이 어긋날 때, "지표의 한계겠지"로 넘어가는 대신 파이프라인을 먼저 의심해야 한다. 이번 경우 **지표가 아니라 파이프라인이 틀렸다.** 흔히 쓰는 파인튜닝 예제 코드를 그대로 따라 쓰면 이 결함이 조용히 들어오며, loss만 보고 있으면 끝까지 발견하지 못한다.

---

## 5. 두 모델 비교 — 디코딩을 greedy로 통일

**왜 loss만으로 부족한가**: 4.3절에서 본 그대로다. 생성문을 직접 채점해야 한다.

**평가지표를 4개 함께 보는 이유**: 챗봇 응답은 정답이 하나가 아니다. `"배고파"`에 `"뭐 좀 드세요"`와 `"얼른 식사하세요"`는 둘 다 좋은 답이지만 표면이 거의 겹치지 않는다. 서로 다른 실패 양상을 잡는 지표를 함께 봐야 한다.

| 지표 | 무엇을 재는가 | 어떤 실패를 잡는가 |
| --- | --- | --- |
| BLEU | n-gram 정밀도 | 정답과 전혀 다른 말을 하는 경우 |
| ROUGE-L | LCS 기반 재현율 중심 F1 | 정답의 핵심 내용을 빠뜨리는 경우 |
| Distinct-1/2 | corpus 단위 n-gram 다양성 | 모든 질문에 같은 답만 하는 모드 붕괴 |
| 빈 응답 비율 | 생성 실패 비율 | 생성 자체의 실패(섞어두면 다른 지표가 왜곡) |
| (보조) 연속 반복률 | 같은 형태소 연달아 나오는 비율 | 응답 내부 토큰 반복 |
| (보조) 평균 길이 | 평균 형태소 수 | BLEU가 짧은 답에 유리해지는 왜곡 — **4단계 EOS 결함의 첫 단서** |

**형태소 단위로 채점하는 이유**: 어절 기준은 조사 하나 차이로 완전 불일치가 된다(`"충전하세요"` vs `"충전 하세요"` = 0점). 음절 기준은 지나치게 후하다(`"사과"`와 `"사람"`이 점수를 받는다). 2.1절에서 얻은 "형태소가 절충점"이라는 결론을 평가에도 동일하게 적용했다. 지표 구현은 "정답을 그대로 생성하면 BLEU 100"과 "무관한 문장이면 0에 수렴"이라는 양 극단 검산으로 검증했다.

**실측 (전체 test 1,175건, greedy 통일)**

| 모델 | epoch | val_loss | BLEU | ROUGE-L | Distinct-1 | Distinct-2 | 평균길이 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 베이스라인 | 117 | 5.7957 | 3.05 | 20.61 | 0.148 | 0.425 | 6.65 |
| **KoBART** | **7** | **2.7359** | **6.85** | **24.36** | **0.232** | **0.574** | 6.17 |

BLEU **+124.6%**, ROUGE-L +18.2%, Distinct-2 +35.1%. **정확도와 다양성이 동시에 올랐고 학습 비용은 오히려 1/17이다.** 보통 이 둘은 상충하는데(다양하게 쓰면 정답에서 멀어진다) 전이학습은 두 축을 함께 끌어올렸다.

---

## 6. 디코딩 전략 비교 후 1개 선택

**왜 별도 축인가**: 학습이 끝난 모델은 매 스텝 확률분포를 내놓을 뿐이고, **그 분포에서 어떤 토큰을 고를지는 학습과 무관한 별개의 결정**이다. 그래서 모델 가중치를 고정한 채 디코딩만 바꿔 비교한다.

**하이퍼파라미터 근거**: `beam_width=5`(NMT에서 4~5가 품질/속도 절충점), `length_penalty=0.7`(beam은 짧은 문장이 유리해지는 편향이 있어 GNMT 방식으로 보정), `temperature=0.8`(분포를 뾰족하게 해 산만한 문장 억제), `top_k=50 / top_p=0.9`(nucleus sampling 표준 조합).

**실측 (전체 test 1,175건)**

| 모델 | 디코딩 | BLEU | ROUGE-L | Distinct-2 | 연속반복률 |
| --- | --- | --- | --- | --- | --- |
| 베이스라인 | greedy | 3.05 | 20.61 | 0.425 | **0.121** |
| 베이스라인 | **beam5** | **4.87** | **22.99** | 0.303 | **0.014** |
| 베이스라인 | top-k50/top-p0.9 | 3.06 | 20.01 | **0.563** | 0.106 |
| KoBART | greedy | 6.85 | **24.36** | **0.574** | 0.028 |
| KoBART | beam5 | 7.07 | 24.09 | 0.481 | 0.027 |
| KoBART | **beam5+no_repeat_ngram3** | **7.10** | 24.12 | 0.482 | 0.026 |
| KoBART | top-k50/top-p0.9 | 4.34 | 22.92 | **0.620** | 0.024 |

**선택 기준을 BLEU로 둔 이유**: 1차 목표는 "질문에 맞는 답을 하는 것"이다. 다양성은 정확도가 확보된 뒤에 의미가 있는 보조 목표다 — 모든 질문에 서로 다른 아무 말이나 하면 Distinct-n은 1.0에 가까워지지만 챗봇으로서는 실패다.

**선택**: 베이스라인 `beam5`, 전이학습 `beam5 + no_repeat_ngram_size=3`. 이후 7·8단계에서 고정한다.

**읽어야 할 세 가지**

1. **베이스라인에서 beam5는 결정적이다.** greedy 대비 BLEU +60%, 연속 반복률 0.121 → 0.014. 학습이 덜 된 모델일수록 근시안적 선택이 누적되는 문제가 크다.
2. **KoBART에서는 디코딩의 영향이 작다**(greedy 6.85 vs beam5 7.07). `no_repeat_ngram_size=3`의 추가 이득은 **0.03**에 불과하다 — EOS를 고친 뒤에는 억제할 반복이 거의 없어 이 설정이 사실상 무효(no-op)가 됐다.
3. **sampling은 두 모델 모두 다양성 최고, BLEU/ROUGE 최저.** 상충 관계가 그대로 드러난다.

> **만약 EOS 결함을 고치지 않았다면** `no_repeat_ngram`이 반복을 크게 줄이는 것처럼 보여 "반복 억제 설정이 결정적"이라는 잘못된 결론에 도달했을 것이다. **디코딩 설정으로 증상을 덮는 대신 학습 쪽 원인을 고친 것이 옳은 순서였다.**

---

## 7. 파인튜닝 하이퍼파라미터 비교

**왜 필요한가**: 4단계 학습 곡선에서 train loss는 계속 내려가는데 val loss가 몇 epoch 만에 반등했다. 소규모 데이터에 빠르게 과적합한다는 신호다.

| 실험 | lr | 스케줄 | B0 대비 바뀐 것 | 가설 |
| --- | --- | --- | --- | --- |
| B0 (기준) | 5e-5 | 고정 | — | — |
| B1 | **2e-5** | 고정 | **lr만** | 갱신 폭을 줄이면 사전학습 지식을 덜 건드려 과적합이 억제된다 |
| B2 | 5e-5 | **warmup+decay** | **스케줄만** | 초기 큰 갱신이 사전학습 가중치를 흔드는 구간을 warmup이 완화한다 |

**공정성 조건**: 셋 다 동일한 학습 예산(상한 10 / patience 3)·배치·시드·데이터·collate·디코딩. 예산이 다르면 "더 오래 학습해서 좋아진 것"과 "설정이 좋아서 좋아진 것"을 구분할 수 없다.

**실측**

| config | 학습 epoch (최적 시점) | val_loss | (순위) | BLEU | (순위) |
| --- | --- | --- | --- | --- | --- |
| B0 lr=5e-5 | 7 (epoch 4) | **2.7359** | 1위 | 7.10 | 2위 |
| B2 lr=5e-5 +warmup | 7 (epoch 4) | 2.7430 | 2위 | 6.32 | **3위** |
| B1 lr=2e-5 | 10 (epoch 7) | 2.7967 | 3위 | **7.28** | **1위** |

**세 config의 val_loss 순위와 BLEU 순위가 전부 어긋났다.** val_loss가 가장 나빴던 B1이 BLEU는 1위다.

> **주의해서 읽을 점**: 셋 다 같은 early stopping 규칙 아래 **정상적으로 조기 종료**했다. B1이 10 epoch까지 간 것은 예산이 부족해서가 아니라 lr이 낮아 수렴이 느려 최적 시점이 늦게(epoch 7 vs 4) 왔기 때문이다 — **B1의 우위는 더 오래 학습한 덕이 아니다.**
>
> 다만 B1(7.28)과 B0(7.10)의 차이는 작고 각 설정을 **시드 하나로 1회씩만** 실행했으므로, 이 순위가 시드 변동 범위 안일 가능성은 배제할 수 없다.

**최종 선택**: B1 (lr=2e-5).

---

## 8. 최종 성능 지표와 오류 분석

### 8.1 최종 순위 (test 1,175건, 디코딩 고정)

| 모델 | 계열 | BLEU | ROUGE-L | Distinct-1 | Distinct-2 | 평균길이 |
| --- | --- | --- | --- | --- | --- | --- |
| **KoBART B1 (lr=2e-5)** | 전이학습 | **7.28** | **24.71** | 0.219 | **0.515** | 6.30 |
| KoBART B0 (lr=5e-5) | 전이학습 | 7.10 | 24.12 | 0.203 | 0.482 | 6.46 |
| KoBART B2 (lr=5e-5+warmup) | 전이학습 | 6.32 | 23.88 | 0.205 | 0.463 | 6.32 |
| KoBART (EOS 결함) | 전이학습 | 5.21 | 19.18 | 0.147 | 0.384 | 12.56 |
| 베이스라인 (117ep) | 베이스라인 | 4.87 | 22.99 | 0.128 | 0.303 | 5.18 |
| 베이스라인 (30ep) | 베이스라인 | 1.70 | 22.44 | 0.021 | 0.055 | 4.25 |

빈 응답은 6개 모델 모두 **0건**. 정답 평균 길이는 6.46형태소다.

### 8.2 개선 추적 — 무엇을 고쳤더니 무엇이 줄었는가

세 변경 모두 **다른 조건을 고정한 통제 실험**이라 원인을 특정할 수 있다.

| 변경 | 바꾼 것 | BLEU | 핵심 지표 변화 |
| --- | --- | --- | --- |
| ① 학습량 확장 | epoch 30 → 117 (그 외 전부 동일) | 1.70 → **4.87** (+186%) | Distinct-1 0.021 → **0.128** (+510%) |
| ② EOS 결함 수정 | 타깃 인코딩 한 줄 (그 외 전부 동일) | 5.21 → **7.10** (+36%) | 구 단위 반복 26.3% → **1.1%**, 정상 종결률 19.0% → **97.5%** |
| ③ 전이학습 도입 | 처음부터 학습 → 사전학습 파인튜닝 | 4.87 → **7.28** (+50%) | Distinct-1 0.128 → **0.219** (+71%) |

**① 학습량 확장**: 30 epoch 시점의 Distinct-1 0.021은 1,175개 질문에 대한 응답 어휘가 거의 전부 겹친다는 뜻이다 — 사실상 모든 질문에 비슷한 답을 하고 있었다. **이 실패는 자동 오류 규칙으로는 잡히지 않고 corpus 단위 Distinct-n으로만 드러난다.**

**② EOS 결함 수정**: 대조군인 베이스라인은 이 수정과 무관하게 값이 그대로다(원래부터 EOS를 붙이고 있었다). **결함이 있던 모델만 정확히 골라서 개선됐다**는 점이 인과를 뒷받침한다.

### 8.3 오류 분석을 두 층으로 나눈 이유

오류에는 규칙으로 셀 수 있는 것과 사람이 읽어야 판정되는 것이 섞여 있다. 섞으면 "대충 이런 것 같다"는 인상비평이 되므로 분리했다.

- **A층 (자동)**: 빈 응답 · 단일 토큰 반복 · 구 단위 반복 · 질문 복사 — 정답을 몰라도 생성문만 보고 판정 가능한 명백한 결함
- **B층 (수동)**: 표면 불일치(정답과 공통 형태소 0개) — 진짜 오답일 수도, 정답과 다를 뿐 타당한 답일 수도 있어 자동 판정 불가

**질문 복사 규칙에서 두 조건을 AND로 건 근거**: 한국어 챗봇 응답은 질문의 단어를 일부 받아 쓰는 것이 자연스럽다(`"배고파"` → `"배고프시군요"`도 정상). 그래서 **대부분이 질문에서 왔고 동시에 정답과는 거의 겹치지 않을 때**만 복사로 판정한다.

### 8.4 B층 수동 판정 — 20건을 직접 읽은 결과

| 수동 분류 | 베이스라인 (117ep) | KoBART B1 |
| --- | --- | --- |
| **타당한 대안 응답** (사실상 오류 아님) | 2건 | **6건** |
| 문맥 무시 | **3건** | 0건 |
| 감정 오독 | **3건** | 0건 |
| 비문 / 불완전 | 2건 | 4건 |

**두 모델의 실패 성격이 근본적으로 다르다.**

- **베이스라인의 실패는 내용 오류다** — 10건 중 6건이 주제를 놓쳤거나 감정을 반대로 읽었다. `"월급 입금됐다고 문자 왔어"` → `"더 좋은 사람 만날 수 있을 거예요."`(기쁜 소식에 이별 위로), `"절약해서 부자돼야지"` → `"슬프네요."`
- **KoBART의 실패에는 내용 오류가 0건이다** — 실패는 전부 비문이고, 나머지는 타당한 답이다. `"상 받을 거 같아"`(정답 `"축하합니다!"`) → `"뿌린대로 거둘 거예요."`는 좋은 답이지만 형태소가 하나도 겹치지 않아 ROUGE-L = 0이 된다.

**두 가지 함의**

1. **KoBART의 BLEU 7.28은 과소평가된 값이다.** 표면 불일치 62건 중 60%가 타당한 응답이라면, 정답이 하나뿐인 데이터로 표면 유사도를 채점하는 방식이 전이학습 모델을 깎아내리고 있다.
2. **표면 불일치 비율이 낮다고 더 나은 모델이 아니다.** 베이스라인의 표면 불일치는 4.1%로 KoBART(5.3%)보다 **적지만** 내용은 훨씬 나쁘다. **A층 자동 규칙만으로 순위를 매겼다면 정반대 결론에 도달했을 것이다** — 수동 판정 층을 따로 둔 이유가 여기에 있다.

### 8.5 감정 label별 성능

| 모델 | label | n | BLEU | ROUGE-L |
| --- | --- | --- | --- | --- |
| 베이스라인 | 0 일상 | 529 | 8.05 | 25.57 |
| 베이스라인 | 1 이별 | 350 | 2.46 | 21.75 |
| 베이스라인 | 2 사랑 | 296 | 2.44 | 19.85 |
| KoBART B1 | 0 일상 | 529 | **12.01** | **28.58** |
| KoBART B1 | 1 이별 | 350 | 3.91 | 22.28 |
| KoBART B1 | 2 사랑 | 296 | 3.80 | 20.68 |

두 모델 모두 label 0(일상)에서 압도적으로 높고 label 1·2는 3분의 1 수준이다. 1단계 데이터 쏠림(일상 5,290 / 이별 3,570 / 사랑 2,963)과 방향이 일치한다. KoBART가 모든 label에서 앞서며, **데이터가 가장 많은 label 0에서 격차가 가장 크다**(12.01 vs 8.05).

---

## 9. 01~08번 실행과 수치가 다른 이유

`PROJECT_FLOW.md`의 수치(2026-09-19, 노트북 01~08)와 이 문서의 수치(2026-09-21, final.ipynb)는 일부 다르다. 원인은 셋이다.

| 항목 | 01~08번 | final.ipynb | 차이의 원인 |
| --- | --- | --- | --- |
| 베이스라인 BLEU | 4.87 | **4.87** | 동일 — 시드 고정이 제대로 동작함을 확인해주는 대조군 |
| 베이스라인 학습 | 117 epoch / 5.7957 | **117 epoch / 5.7957** | 동일 |
| 최고 전이학습 | B2 (warmup), 7.90 | **B1 (lr=2e-5), 7.28** | ① 학습 예산 통일 ② 실행 순서에 따른 셔플 RNG 차이 |
| EOS 수정 전 KoBART | BLEU 3.83 | BLEU 5.21 | 별도로 학습된 다른 실행이고 진단 수치의 측정 조건(표본 크기·디코딩)도 달라, 두 값은 직접 비교 대상이 아니다. 결함의 양상(문장이 끝나지 않음)은 양쪽에서 동일하게 관찰됐다 |

1. **학습 예산을 통일했다.** 01~08번에서는 04번(B0)이 상한 10, 06번(B1/B2)이 상한 7이었다. final.ipynb는 세 config 모두 상한 10 / patience 3으로 맞춰 **같은 조건에서 비교**한다. 예산이 늘어난 B1이 최적 시점 epoch 7까지 도달하면서 순위가 바뀌었다.
2. **한 노트북에서 순차 실행하므로 셔플 RNG 상태가 다르다.** 모델 초기화는 시드를 고정했지만 DataLoader 셔플은 전역 RNG를 쓰므로, 앞선 학습이 소비한 난수만큼 배치 순서가 달라진다. val_loss가 소수 셋째 자리에서 달라지는 이유다.
3. **베이스라인 계열이 완전히 동일하다는 점이 중요하다.** 데이터 분할·BPE·Word2Vec·베이스라인 학습이 모두 같은 값을 재현했으므로, 차이는 전이학습 쪽의 위 두 요인으로 한정된다.

**결론의 방향은 두 실행에서 동일하다**: 전이학습이 베이스라인을 크게 앞서고, EOS 결함 수정이 단일 변경으로 가장 큰 개선을 냈으며, val_loss 순위가 생성 품질 순위를 보장하지 않는다.

---

## 10. 과제 요구사항 대응표

`CLAUDE.md`의 Project 절 항목을 어디에서 다루는지 정리한다.

| 요구사항 | final.ipynb 위치 | 이 문서 |
| --- | --- | --- |
| 토큰화 — 무엇을 선택했고 근거는 | 2.1~2.4 | [2.1](#21-토큰화-단위--왜-넷을-다-재봤는가) |
| BPE 토큰화 | 2.2 | [2.2](#22-왜-서브워드bpe인가) |
| 임베딩 step by step 흐름 | 2.5 | [2.4](#24-임베딩--단계별로-드러내기) |
| 워드투벡터 | 2.5 | 2.4 |
| 인코더·디코더 구조 선택 근거 / 디코더 전용 | 3.1 | [3.1](#31-구조-선택--세-후보를-모두-검토한-뒤-고름) |
| 엘렉트라 | 3.1 (쓰지 않는 근거) | 3.1 |
| 전이학습 활용 | 4 | [4](#4-전이학습--kobart-파인튜닝-그리고-결함-규명) |
| 트랜스포머 다운로드 파이프라인 설계 | 4.1~4.2 | [4.2](#42-파이프라인-설계-6단계) |
| 파인튜닝 | 4.5, 7 | 4.5, 7 |
| 하이퍼파라미터 비교 실험 | 7 | [7](#7-파인튜닝-하이퍼파라미터-비교) |
| 디코딩 전략 (접근법) | 6 | [6](#6-디코딩-전략-비교-후-1개-선택) |
| 평가지표 — 왜 이 지표인가 | 5.1 | [5](#5-두-모델-비교--디코딩을-greedy로-통일) |
| 베이스라인을 뭘 고쳤을 때 뭐가 개선되었는지 | 4.3~4.5, 8.4 | [4.5](#45-수정과-통제-실험-결과), [8.2](#82-개선-추적--무엇을-고쳤더니-무엇이-줄었는가) |
| 오류분석 | 8.3~8.6 | [8.3](#83-오류-분석을-두-층으로-나눈-이유), [8.4](#84-b층-수동-판정--20건을-직접-읽은-결과) |
| 평가하고 결과 발표 | 8.7 + 보고서 | 8 |

---

## 11. 재현 방법

```
notebooks/final.ipynb 를 커널 `nlp-project-venv` 로 열고 위에서부터 전부 실행
```

- 소요 시간: 약 24분 (RTX 4070 Ti 12GB 기준)
- 필요 입력: `dataset/ChatbotData.xlsx` 하나
- 생성물: `dataset/processed/`(전처리·토크나이저·임베딩), `checkpoints/final/`(모델 6종), `results/final/`(지표·예측·그래프 33개 파일)
- 시드는 전 단계 고정되어 있어, 같은 환경에서는 같은 값이 재현된다(9장 3번 항목 참고)

**주의**: `results/`(01~08번 산출물)와 `checkpoints/`의 다른 파일은 건드리지 않는다. final.ipynb는 `final` 하위 폴더만 쓴다.

---

## 12. 남은 과제

| 우선순위 | 할 일 | 근거 |
| --- | --- | --- |
| 1 | 사람 평가 또는 의미 기반 지표(BERTScore 등) 추가 | 8.4절 — KoBART 표면 불일치의 60%가 타당한 응답. 현재 BLEU는 과소평가된 값이다 |
| 2 | KoBART의 비문 생성 개선 | 8.4절 — KoBART 실패의 전부가 비문. 내용이 아니라 생성 안정성 문제 |
| 3 | 베이스라인의 문맥 무시·감정 오독 개선 | 8.4절 — 베이스라인 실패의 주종. 모델 용량·학습량 문제일 가능성 |
| 4 | 시드를 바꿔 하이퍼파라미터 실험 반복 | 7장 — B1(7.28)과 B0(7.10)의 차이가 작아 시드 변동 범위 안일 가능성 |
| 5 | lr을 2e-5보다 더 낮춰(1e-5) 추가 탐색 | 7장 — lr을 낮춘 B1이 BLEU 1위. 이 방향에 추가 이득이 있는지 |
| 6 | label 1·2 성능 보강 (가중 샘플링 등) | 8.5절 — label 0 대비 BLEU가 3분의 1 수준 |
| 7 | label을 조건으로 주는 구조 실험 | 1장 미해결 항목 — (Q, A) 동일·label 상이 행의 활용 |
